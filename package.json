{
  "name": "Xbox-One-Electron",
  "version": "0.1.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "DEBUG=smartglass:* electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "electron-builder -c.mac.identity=null --dir",
    "dist": "electron-builder -c.mac.identity=null"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.2",
    "electron-builder": "^21.2.0"
  },
  "dependencies": {
    "xbox-smartglass-core-node": "^0.6.1"
  },
  "build": {
    "appId": "com.unknownskl.xbox-one-electron"
  }
}
